{
  "name": "guard-relay-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "wrangler types && tsc -p tsconfig.worker.json --noEmit",
    "audit": "npm audit --omit=dev"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "@simplewebauthn/server": "13.3.1"
  }
}
